{
  "name": "latex-cv-builder",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server/server.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "build": "cd client && npm run build",
    "preinstall": "cd client && npm install"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "latex-cv-builder": "file:",
    "mongoose": "^8.7.0"
  },
  "devDependencies": {
    "mocha": "^9.0.0"
  }
}
