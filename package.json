{
  "name": "latex-cv-builder",
  "version": "1.0.0",
  "engines": {
    "node": "14.x"  // Specify your Node.js version here
  },
  "scripts": {
    "start": "node server/server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {
    // Move all server dependencies here
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.7.0"
  },
  "devDependencies": {
    "mocha": "^9.0.0"
  }
}
